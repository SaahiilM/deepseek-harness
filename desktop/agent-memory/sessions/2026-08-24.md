# Agent session log — 2026-08-24

## 13:27 — note
round 17: adoption escape hatch. Problem: ServerController unconditionally adopts any live harness server probing 3080+lastOwned, so while a master-checkout server hosts an unrelated chat session the app could never spawn its own worktree server (phone would be served master code). Added DSH_DESKTOP_SPAWN_OWN env var + DSHDesktopSpawnOwn defaults mirror (defaults channel is reliable for GUI launches; open --env silently failed to deliver). Verified E2E: owned spawn port 58321 cwd=dsh-desktop-wt --directory-picker browse, one fresh gate, chat-host server on 3080 untouched. Learned: launching GUI apps directly from a sandboxed shell poisons them (WebKit cache + node discovery denied) - always relaunch via open/launchd.
