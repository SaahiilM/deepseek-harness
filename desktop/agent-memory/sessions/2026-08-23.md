# Agent session log — 2026-08-23

## 23:05 — branch bootstrap
- Did: created `desktop-app` off `master` (origin default; repo uses `master`, not `main`).
- Did: scaffolded `desktop/` — PROJECT_MEMORY.md, agent-memory/, scripts (sync-origin,
  build, run, log-session, make-icon), Swift shell sources, Info.plist, README.
- Decided: everything additive under `desktop/` so upstream rebases stay clean and
  repo gates never see our files.

## 23:21 — Swift shell compile fixes
- Learned: `swiftc` accepts only one `-target` per invocation — universal binaries
  need two compiles + `lipo -create`. Multiple `-arch` flags are not enough to set a
  deployment target.
- Learned: `-parse-as-library` forbids top-level statements, so `main.swift` cannot
  exist alongside it. Used `@main` on AppDelegate instead.
- Learned: **`@main` on an NSApplicationDelegate compiles but does NOT install the
  delegate** when the binary runs without nib/principal-class wiring — process runs,
  window never appears, zero output. Fixed with explicit `static func main()` that
  builds NSApplication, installs the delegate, and calls `run()`.
- Learned: NSObject inherits an instance method named `bind(_:)` (legacy Cocoa
  bindings) — the POSIX socket call must be spelled `Darwin.bind`.

## 23:28 — server spawn verification (headless)
- Did: verified `node --import tsx/esm apps/cli/src/bin.ts web --no-open --port N`
  boots and serves HTTP 200 from this checkout (fresh smoke on port 3457).
- Learned: port 3080 on this machine is the LIVE harness GUI session hosting our
  conversation — never kill pid serving 3080 (`node --import tsx/esm apps/cli/src/bin.ts web`
  started Fri). The app's free-port picker correctly avoided it.
- Learned: `/usr/local/bin/node` here is a stale v16 that rejects `--import`; the
  real Node lives under `~/.nvm/versions/node/`. locateNode() now scans nvm versions,
  validates engines (^22.19 || >=24) by running `--version`, and prefers newest.
- Learned: first launch of the bare binary from /tmp showed the designed error page
  (no checkout found above /tmp) — walk-up resolution works; env `DSH_DESKTOP_REPO`
  override confirmed working.

## 23:34 — app bundle assembled and verified
- Did: generated AppIcon.icns via shell/Tools/make-icon.swift (CoreGraphics render +
  iconutil).
- Learned: `iconutil` rejects any folder not named `*.iconset` ("Invalid Iconset").
- Learned: NSBitmapImageRep has no `resized(to:)`; scale by drawing the NSImage into
  a new lockFocus'd NSImage at target size, then tiff → rep → png.
- Did: `build.sh --shell-only` produced universal bundle (lipo confirms x86_64+arm64),
  ad-hoc codesigned at desktop/dist/DeepSeek Harness.app.
- Did: launched the .app via `open` — spawned server on free port 52096 with
  nvm v24.14.1, UI answered HTTP 200, AppleScript quit terminated BOTH app and child.
- Next: commit; then validate the FULL build.sh path (pnpm install + pnpm run build)
  once, accepting that rebuilding web dist triggers a reload of the live GUI session.

## 23:40 — hygiene note for future sessions
- Never `pkill -f node` broadly: VS Code helpers, bb.app daemons, and the live
  harness GUI all match. Target patterns precisely (`bin.ts web --no-open --port N`).
- Test instances accumulate: each relaunch leaves a window; `pgrep | head -1` kills
  only one. Verify with full ps listing after cleanup.

## 23:52 — full build validated, wrap-up
- Did: ran `desktop/scripts/build.sh` (full path) — pnpm install, harness build,
  universal Swift compile, bundle assembly, ad-hoc codesign. Exit 0.
- Did: relaunched the rebuilt app; served HTTP 200 on a fresh free port; AppleScript
  quit terminated both processes cleanly.
- Did: added optional `install-sync-timer.sh` (LaunchAgent, daily 09:00 + at login)
  for hands-off origin/master syncs; NOT installed — user opts in with one command.
- Learned: rebuilding apps/web/dist while the live GUI session (port 3080) serves
  that tree is safe — the running server keeps its in-memory artifacts; only its
  browser page would refresh on next poll.
- Next session: start with PROJECT_MEMORY.md; open items are the v2 ideas
  (self-contained node bundle, dock presence, notifications, DMG packaging).

## 00:10 — round 2: reference-product study + parity features
- Did: actually studied the references (first session's web_search had no API
  key — admitted to user). Cloned pingdotgg/t3code + get-bb/bb to ../research/;
  dissected installed /Applications/bb.app (Electron, dev.bb.desktop).
- Learned: both are "local server + web UI + thin native wrapper" — validates
  our architecture. bb: single-instance lock, window-state.json, Edit/zoom
  menus, New Window, about panel, auto-update, server-probe splash. t3code:
  dock icon set at runtime, connecting splash on dock activation, settings.
- Did: implemented parity set — single instance, frame persistence,
  Edit/View/File menus, New Window, SessionMonitor (dock badge + finish
  notifications via POST /api/session.list every 3s + dock-bounce fallback),
  make-dmg.sh (947KB DMG vs bb's ~200MB Electron bundle).
- Learned (hard-won): UNUserNotificationCenter.current() ABORTS a bare binary
  with no bundle id — guard on Bundle.main.bundleIdentifier. TCC denies
  notification permission to ad-hoc-signed apps entirely ("not allowed for
  this application") — dock bounce is the reliable fallback.
- Learned: stale-bundle trap — `open` launches whatever is in dist/; two
  confusing "no traffic" debugging rounds came from testing a bundle built
  before the logging existed. Always rebuild before behavioral tests.
- Learned: `running` in session.list is per-host live state; the app's badge
  tracks agents launched through its own server (correct Codex-like scope).
  Approval-wait state needs the /api/events.mux stream (deferred).
- Verified: bundled-mode logs show monitor start + "poll ok, 12 item(s)";
  single-instance (second open → still 1 process); DMG builds.
- Next: self-contained node bundle, approval-needed signal, signing.

## 00:30 — round 3: standards pass (user directive)
- Did: user required app-dev/codebase standards — no god files. Split 4 files
  into 14 modules with single responsibilities:
  AppMain/SingleInstanceGuard, AppDelegate (composition root only),
  AppMenuBuilder (@objc MenuActions protocol), WindowManager/WindowController,
  LifecyclePages (pure), ServerController (orchestration only),
  RepoLocator/NodeLocator/FreePortPicker/ServerLogStore/ReadinessProbe,
  SessionListWire (pure codec), SessionMonitor + AgentPresenceReporting
  protocol (SystemPresenceReporter implements; monitor now testable via spy).
- Did: build.sh now globs Sources/*.swift (new files need no script edits);
  added desktop/scripts/test-shell.sh — compiles all sources minus AppMain
  plus Tests/main.swift (top-level entry) and runs 27 behavior checks.
- Learned: tests caught 2 REAL bugs immediately: escapeHTML applied & after <
  (double-encoded entities), log tail counted trailing newline as an empty
  line. Also > was never escaped in tail lines. Fix-first-then-test would
  have shipped all three.
- Learned: Swift trivia — top-level statements require a file literally named
  main.swift AND exclusion of the module's @main type; `Self.` in default
  init arguments is rejected (covariant Self).
- Verified: 27/27 checks pass; rebuilt bundle launches; stderr shows monitor
  start → one non-200 failure logged once → recovered → poll ok (13 items);
  UI HTTP 200.

## 00:40 — round 5: backend adoption (user's architecture question)
- User asked why browser-run threads don't show active in the native app, and
  whether the two are separate backends or clients.
- Answer established: they were separate `dsh web` host processes sharing one
  durable store; `running` in session.list is per-host LIVE state (proved
  empirically: app's host listed this conversation as running:false while it
  ran on 3080).
- Fix: adopt-over-spawn (bb's server-probe/owned-runtime pattern). New
  ServerProbe fingerprints a harness host via its session.list RPC envelope;
  ServerController probes 3080 + last-owned port, attaches without owning,
  spawns only when nothing answers, and never terminates an attached server.
- Verified live: app launched while 3080 GUI runs → log shows "attached to
  existing harness server at http://127.0.0.1:3080/", monitor "running
  agents: 3" — cross-client visibility works; no new server spawned.
- Learned: orphaned owned servers accumulate when the app is SIGKILLed or
  crashes (Abort path skips applicationWillTerminate). Adoption makes this
  self-healing (orphans get reused), but process-group kill would be the hard
  fix. Cleaned four orphans manually; production 3080 spared.

## 00:40 — round 5: backend adoption (user architecture question)
- User asked why browser-run threads do not show active in the native app,
  and whether the two are separate backends or clients.
- Answer established: separate dsh web host processes sharing one durable
  store; running in session.list is per-host LIVE state (proved empirically:
  app host listed this conversation running:false while it ran on 3080).
- Fix: adopt-over-spawn (bb server-probe/owned-runtime pattern). New
  ServerProbe fingerprints a harness host via its session.list RPC envelope;
  ServerController probes 3080 + last owned port, attaches without owning,
  spawns only when nothing answers, never terminates an attached server.
- Verified live: app launched while 3080 GUI runs -> attached to
  http://127.0.0.1:3080/, monitor reported running agents: 3 (this very
  conversation included). No new server spawned.
- Learned: orphaned owned servers accumulate when the app crashes or is
  SIGKILLed (no applicationWillTerminate); adoption makes reuse self-healing,
  but process-group kill would be the hard fix. Cleaned four orphans;
  production 3080 spared.

## 01:00 - round 6: approval-needed signal over the mux websocket
- Wire ground truth (captured live): GET /api/events.mux upgrades to a
  WebSocket (the SSE path exists only in the pure fetch handler; deployed
  server answers plain GET with 426). Frames are JSON text messages
  {type:"server-request",rpcId,method,payload:{type,...}}. Approval asks ride
  payload.type "approval/requested" {sessionId,approvalId,toolName}; mux-open
  REPLAYS still-pending asks with stable rpcIds (api-proxy pendingApprovals),
  resolutions broadcast payload.type "approval/resolved". Answers go out of
  band via POST /api/respond - the shell observes, never answers.
- Did: ApprovalStream module (URLSessionWebSocketTask receive loop, serial
  state queue, backoff reconnect capped 30s relying on replay for healing);
  AgentPresenceReporting gained approvalsChanged(pending:) - reporter diffs
  new ids -> per-approval notification, badge policy "! beats count beats
  none"; AppDelegate shares one SystemPresenceReporter across monitor +
  stream; restart/failed/exited stop the stream.
- Learned: URLSessionWebSocketTask to ws://127.0.0.1:3080/ drops instantly -
  the upgrade handler only accepts the exact path /api/events.mux; scheme
  swap must also set components.path. Node ws client worked from repo root
  only via node_modules/.pnpm/ws@*/node_modules/ws (pnpm strict layout).
- Verified: 36/36 checks (8 new parser/scheme tests incl. golden frames);
  app attached to 3080, two ESTABLISHED sockets (HTTP poll + WS), no
  reconnect churn over 20s. Real-approval E2E unverified - needs a session
  turn that trips permission; headless run needs DEEPSEEK_API_KEY (absent).
